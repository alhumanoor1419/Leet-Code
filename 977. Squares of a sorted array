class Solution:
    def sortedSquares(self, nums: List[int]) -> List[int]:
        lst=[]
        for val in nums:
            sqrt=val*val
            lst.append(sqrt)
        lst.sort()
        return lst
