class Solution:
    def missingNumber(self, nums: List[int]) -> int:
        n=len(nums)
        actual_output=n*(n+1)//2
        expected_output=sum(nums)
        return actual_output - expected_output
        
